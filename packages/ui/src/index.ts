// @cmmp/ui: shared React component library -- scaffolded (see package.json's
// dependencies) but no components have been built here yet. apps/web's own
// components/ui/ (shadcn-style primitives) currently fill this role instead.
export {};
